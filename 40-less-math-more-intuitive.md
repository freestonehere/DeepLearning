# 代码中的 `net` 对应数学上自回归模型的哪一部分？
- 完全看懂需要很强的数学功底！
  - 不要纠结这个了！
  - 不过也不需要完全看懂！
- **能从直观上理解代码就行了！**
- **从数学上理解代码是研究员做的事，我的水平还不到！**
- 内容来自 AI

## 一、对条件概率建模——自回归模型

正向分解告诉我们，要得到整个序列的联合分布，只需要估计每一个条件概率 $p(x_t \mid x_1, \dots, x_{t-1})$ 。但直接估计这些条件概率在高维空间中是困难的，通常我们会引入一个参数化模型 $f$ 来近似这个条件分布：

$$p(x_t \mid x_1, \dots, x_{t-1}) \approx p\big(x_t \mid f(x_1, \dots, x_{t-1})\big)$$

这里的 $f$ 是一个**确定性函数**，它根据历史信息输出一个表示（例如分布的参数），然后由这个表示决定 $x_t$ 的分布。这种建模方式称为**自回归模型**（Autoregressive Model），因为模型使用自身的过去值来预测当前值。

## 二、代码是如何体现上述公式的
结合公式 $p(x_t \mid x_1, \dots, x_{t-1}) \approx p\big(x_t \mid f(x_1, \dots, x_{t-1})\big)$ 来看，这里的 **$f$ 是一个函数**，它根据历史观测 $x_1, \dots, x_{t-1}$ 输出一个表示（例如分布的均值、方差等），然后利用这个表示来定义 $x_t$ 的条件分布。在代码中，这个函数 $f$ 就是通过 `get_net()` 定义并训练的**神经网络 `net`**。

具体来说：
- `net` 是一个多层感知机（MLP），其输入维度为 `tau=4`（即使用最近 `tau` 个历史值作为输入，这是基于马尔可夫假设的简化，相当于用 $x_{t-4}, \dots, x_{t-1}$ 来近似完整历史），输出维度为 1。
- 在训练时，`net(X)` 接收形状为 `(batch_size, tau)` 的输入 `X`，输出对下一个时刻值的预测。这个预测值就是 $f(x_1,\dots,x_{t-1})$ 的结果。
- 代码使用均方误差（MSE）作为损失函数，这等价于假设 $x_t$ 服从均值为 $f(\cdot)$、方差固定的高斯分布，即 $p(x_t \mid f(\cdot)) = \mathcal{N}(x_t \mid f(\cdot), \sigma^2)$。因此，`net` 直接输出了条件分布的均值参数，而方差则隐含在损失函数中（未显式建模）。
- 在预测阶段，无论是单步预测还是多步预测，都是通过调用 `net(...)` 来获得下一个值的预测，这正是 $f$ 在发挥作用。

所以，代码中的 **`net` 就是公式里的模型 $f$**，它从历史数据中学习映射关系，并用其输出参数化条件概率分布。

## 三、针对【二】中的部分问题做详细解释
在自回归模型中，我们希望建模条件概率 $p(x_t \mid x_1, \dots, x_{t-1})$，即给定过去所有观测，当前值的分布。直接建模这个高维条件分布非常困难，因此我们引入一个函数 $f$，用它来概括历史信息，并假设条件分布的形式为 $p(x_t \mid f(x_1, \dots, x_{t-1}))$。这里的 $f$ 将历史映射到一个较简单的表示（例如一个实数），然后基于这个表示定义 $x_t$ 的分布。

在代码中，这个 $f$ 具体表现为一个多层感知机 `net`。下面我们结合训练过程和损失函数，详细解释这两段话的含义。


#### 1. `net(X)` 与 $f(x_1,\dots,x_{t-1})$ 的关系

- **输入 `X` 的构造**：  
  代码中使用了一个 **马尔可夫假设**（阶数 `tau=4`），即假设当前值只依赖于最近的 `tau` 个历史值，而不依赖更早的观测。因此，条件概率简化为  
  
  $$p(x_t \mid x_1, \dots, x_{t-1}) \approx p(x_t \mid x_{t-\tau}, \dots, x_{t-1})$$

  训练时，每个样本 `X` 是一个形状为 `(batch_size, tau)` 的张量，每一行对应一个时间步的最近 `tau` 个历史值：  
  
  $$X[i,:] = [x_{i}, x_{i+1}, \dots, x_{i+\tau-1}] \quad (\text{这里索引需对照代码中的滑动窗口})$$

  对应的标签 `y[i]` 是下一个时刻的真实值 $x_{i+\tau}$ 。

- **`net(X)` 的输出**：  
  `net` 是一个多层感知机，输入维度为 `tau`，输出维度为 1。对于每个输入样本（历史窗口），网络输出一个标量值，记作 $\hat{x}_t$。这个 $\hat{x}_t$ 就是函数 $f$ 在给定历史上的输出，即  
  
  $$f(x_{t-\tau}, \dots, x_{t-1}) = \hat{x}_t$$

  在代码中，`net(X)` 一次性计算整个批次的输出，其结果就是所有样本对应的预测值。

因此，**`net` 直接扮演了函数 $f$ 的角色**：它接收历史信息，输出一个用于预测 $x_t$ 的数值。

#### 2. 均方误差（MSE）损失与高斯分布假设

##### 为什么 MSE 能用于训练概率模型？

在概率建模中，我们通常希望最大化观测数据的似然。对于自回归模型，给定历史 $x_{<t}$，观测到 $x_t$ 的条件概率为 $p(x_t \mid x_{<t})$。假设我们采用参数化模型 $p_\theta(x_t \mid x_{<t})$，则整个序列的似然为

$$p_{\theta}(\mathbf{x}) = \prod_{t} p_{\theta}(x_t \mid x_{<t})$$

最大化这个似然等价于最小化负对数似然（NLL）：

$$-\log p_\theta(\mathbf{x}) = -\sum_t \log p_\theta(x_t \mid x_{<t})$$

##### 将 MSE 与高斯分布联系

如果我们假设条件分布 $p_\theta(x_t \mid x_{<t})$ 是高斯分布，且其均值由历史决定，方差为常数，即

$$p_\theta(x_t \mid x_{<t}) = \mathcal{N}\big(x_t \mid \mu_\theta(x_{<t}), \sigma^2\big)$$

其中 $\mu_\theta(x_{<t})$ 是某个参数化函数（例如我们的神经网络 $f$）， $\sigma^2$ 是固定的方差（与输入无关）。那么，单步的负对数似然为：

$$-\log p_\theta(x_t \mid x_{<t}) = \frac{1}{2\sigma^2} \big(x_t - \mu_\theta(x_{<t})\big)^2 + \frac{1}{2}\log(2\pi\sigma^2)$$

忽略常数项 $\frac{1}{2}\log(2\pi\sigma^2)$ 和缩放因子 $\frac{1}{2\sigma^2}$，最小化负对数似然就等价于最小化 $(x_t - \mu_\theta(x_{<t}))^2$ —— 这正是均方误差！

##### 代码中的体现

- 代码使用的损失函数是 `nn.MSELoss(reduction='none')`，它对每个样本计算平方误差：  
  
  $$\ell = (y - \text{net}(X))^2$$

  然后，在训练循环中，我们对一个批次的损失求和并反向传播：`l.sum().backward()`。这相当于最小化所有样本的平方误差之和，即最小化整个训练集的 MSE。

- 这个做法**隐含地**假设了每个 $x_t$ 给定历史后服从均值为 `net(X)`、方差为常数的高斯分布。虽然代码中没有显式写出概率公式，但通过选择 MSE 损失，我们实际上在进行最大似然估计，且模型输出的正是高斯分布的均值参数。

- 方差 $\sigma^2$ 没有显式建模，它被吸收到损失函数中作为一个常数因子（因为优化时 $\sigma^2$ 不影响最优的 $\mu_\theta$）。如果需要，后续可以通过计算残差方差来估计 $\sigma^2$，但在预测阶段通常只关心均值。

##### 为什么可以这样？

在回归任务中，MSE 是最常用的损失函数，其背后的概率解释就是高斯噪声假设。因此，即使不刻意强调概率视角，我们的神经网络仍然在学习条件期望 $\mathbb{E}[x_t \mid x_{<t}]$，这正是高斯分布下的均值。所以，**`net` 的输出就是对条件概率均值的一个估计**，从而间接实现了对条件概率的建模。

#### 总结

- **模型 $f$** 就是神经网络 `net`，它接收最近 `tau` 个历史值，输出下一个值的预测。
- **MSE 损失** 等价于假设 $x_t$ 给定历史后服从均值为预测值、方差固定的高斯分布，因此训练过程实际上是在最大化条件似然。
- 尽管代码中没有显式写出概率公式，但通过这种损失函数的选择，我们隐式地完成了概率建模，使得训练出的网络能够用于预测和后续的多步采样。

## 四、模型 `f` 输出的明明不是预测结果吗？怎么就成均值了？
你问得很好！这里的“预测结果”和“均值”其实是一回事，只是从不同角度理解。让我用数学和直观的方式解释清楚。

### 1. 什么是“均值”？

在概率论中，如果一个随机变量 $Y$ 服从某个分布，那么它的**均值（期望）**就是该分布的中心位置。例如，高斯分布 $\mathcal{N}(\mu, \sigma^2)$ 的均值就是 $\mu$。当我们说“预测结果”时，通常是指对 $Y$ 的一个**点估计**——一个具体的数值，用来代表对 $Y$ 的猜测。

在自回归模型中，我们希望预测下一个值 $x_t$。如果假设 $x_t$ 是一个随机变量，那么最合理的点估计就是它的条件期望 $\mathbb{E}[x_t \mid \text{历史}]$。而这个条件期望恰好就是条件分布的均值。

### 2. 在代码中，为什么 `net` 的输出既是预测结果又是均值？

#### 从损失函数看
代码中使用的是**均方误差（MSE）损失**：

$$\ell = (y - \hat{y})^2,$$

其中 $y$ 是真实值，$\hat{y} = \text{net}(X)$ 是网络的输出。在训练中，我们希望让 $\hat{y}$ 尽可能接近 $y$。

学过统计学的同学都知道：**最小化均方误差的最优解，就是条件期望**。也就是说，如果模型容量足够大、训练数据无限，那么训练好的网络 $f$ 会逼近：

$$f(x_{<t}) \approx \mathbb{E}[x_t \mid x_{<t}]$$

这个条件期望 $\mathbb{E}[x_t \mid x_{<t}]$ 正是 $x_t$ 在给定历史后的分布均值。

#### 从概率建模看
我们之前提到，使用 MSE 损失等价于假设：

$$x_t \mid x_{<t} \sim \mathcal{N}\big(\mu(x_{<t}), \sigma^2\big),$$

其中 $\mu(x_{<t})$ 就是我们要学习的函数。此时，**条件分布的均值 $\mu(x_{<t})$ 就是 $\mathbb{E}[x_t \mid x_{<t}]$**。而网络 $f$ 直接输出这个 $\mu$，所以 $f$ 的输出既是“预测结果”（因为我们就用这个值作为对 $x_t$ 的猜测），也是“均值”（因为它是条件分布的均值参数）。

### 3. 直观理解

假设我们想预测明天的气温。如果我们知道历史气温，我们可以给出一个具体的数值，比如 25°C。这个 25°C 就是我们的**预测结果**。但从概率角度看，明天的实际气温可能围绕 25°C 波动，因此 25°C 就是条件分布的**均值**。在 MSE 损失下，我们学到的 $f$ 就是直接输出这个均值。

如果损失函数换成交叉熵（用于分类），那么网络输出的就是类别概率，不再是均值。但在回归任务中，MSE 下网络的输出就是条件期望，也就是均值。

### 4. 代码中没有显式写“均值”的原因

代码直接用了 `net(X)` 输出一个数值，然后和真实值计算 MSE。它没有显式说“这个输出是均值”，因为在实际编程中，我们通常只关心预测值本身，而不需要显式地把它当作分布参数。但在概率解释中，这个数值就是条件分布的均值。

所以，你的理解没错：**`net` 输出的确实是预测结果，而这个预测结果在概率模型下就是条件分布的均值**。两者是同一个东西，只是视角不同。

希望这个解释能消除你的疑惑！如果还有问题，欢迎继续探讨。