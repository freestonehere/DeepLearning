# ① 数值稳定性 & ② 权重初始化 ( weight-init ) 和 激活函数 ( activation-function )

## 一、概念理解
### （一）、数值稳定性：梯度爆炸 和 梯度消失
1. 标量关于行向量的导数是列向量
   1. 但 Pytorch 好像统一将列向量处理为行向量？
2. 向量关于向量的导数是矩阵
3. 矩阵之间的求导相关的知识补充？
   1. 不需要会推导
   2. 但起码理解起来要自然！

- **数学虽然目前用不到，但绝对不能忽视。尽可能多的理解！**

#### 1、梯度爆炸

<br>

#### 2、梯度消失

<br>

1. 为了计算性能，常用 16 bit 数，
   1. 但 16 bit 数表示的范围有限
   2. ⇒ 一旦神经网络变深 并且 学习率设置不当，那么就**非常容易**导致梯度爆炸 / 梯度消失
   3. ⇒ 也就是说，学习率的设置范围其实非常小

<br><br><br>

### （二）、权重初始化和激活函数
#### 1、让训练更稳定
- 目标：让梯度值在合理的范围内（例如 $[1e-6,1e3]$ ），以保证训练过程更加平滑，减少梯度爆炸和梯度消失的可能性。目前主要方法有：
  1. 将乘法变加法：ResNet、LSTM
  2. 归一化：梯度归一化（ Normalize ），梯度剪裁（ Clipping ）
  3. **合理的权重初始和激活函数**（ 本节内容，前面两个以后会讲 ）

<br>

#### 2、权重初始化
- Batch Normalization 确实可以达到，让输出均值为 0、方差一致
  - 但 BN 不一定保证梯度（以后再说）
1. （**前向计算**）要保证**输入方差与输出方差**也相等，即 $Var[h_i^t]=Var[h_j^{t-1}]$ ，则： $n_{t-1}\gamma_t=1$
   1. $\gamma_t$ 是第 $t$ 层参数的方差
   2. $n_{t-1}$ 是输入维度， $n_{t}$ 是输出维度
2. （**反向传播**）要使**每层梯度的方差**相等，根据以上推导可知，须令 $n_t\gamma_t=1$

##### (1)、Xavier 初始化（随机初始化）
1. 综上，要想满足每层神经网络输出和梯度的方差均为常数，权重初始化要满足：
   - $n_{t-1}\gamma_t=1$（即每层**前向输出方差**保持**一致**的条件）
   - $n_t\gamma_t=1$（即每层**反向传播**梯度**方差**保持**一致**的条件）
2. 但是难以取得一个方差 $\gamma_t$ ，使之同时满足 $n_{t-1}\gamma_t=1$ 和 $n_t\gamma_t=1$ 。因为 $n_{t-1}$ 和 $n_t$ 的值是不可控制的。除非输入维度等于输出维度。
3. 这里提出一个折中的方法 —— **Xavier** ：
   - 首先令： ${{(n_{t-1}+n_t)\over2}\gamma_t}=1\ \Rightarrow \gamma_t={2\over(n_{t-1}+n_t)}$
     - 在初始化第 $t$ 层时，权重采用正态分布 $\mathscr{N}(0,\sqrt{2/(n_{t-1}+n_t)})$
     - 或采用均匀分布 $\mathscr {U}\left(-\sqrt{6/(n_{t-1}+n_t)},\sqrt{6/(n_{t-1}+n_t)}\right)$

<br>

#### 3、激活函数
1. 要保证**反向传播**时输出的 **期望为 0、方差相等**，则： $\alpha=1，\beta=0$ ，
   1. 选取的线性激活函数必须为 $f(x)=x$ 形式。
2. 常用非线性激活函数
   1. $$\text{scaled sigmoid}(x)=4\times\sigma(x)-2$$
   2. $$\text{ReLU}(x)=\max \{ 0, x \}$$
   3. $$\text{tanh}(x)$$

##### (1)、常用的非线性激活函数
- 另外，补充一个现象：往往是，深度学习的**权重一般就是在 0 附近**设置。
  - ⇒ 所以，这里可以用泰勒级数在 0 处展开。
1. 使用非线性激活函数时，如果要使得神经网络正向输出和反向梯度满足均值为零，方差为定值的要求，
2. 就得采用那些**经泰勒级数展开后，一阶项系数为 1 的函数**（比如 ReLU、TanH、Scaled-Sigmoid），
3. 此时该激活函数在原点附近可以近似看作是**恒等函数**（ Identity Function $f(x) = x$ ）。但超出了零点附近区间，会使其均值、方差发生变化。

### （三）、有意思的问题
1. 输出 / 参数符合正态分布有利于深度学习吗？
   1. 这个说法不对。
   2. 因为我们想让相关的东西分布在一定的范围内
   3. 而随机分布又容易计算期望 / 方差
   4. 所以，随便假设一个随机分布（比如：高斯分布）就行

