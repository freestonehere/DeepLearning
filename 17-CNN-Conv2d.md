# CNN - Conv2d （卷积神经网络-卷积层）
## 一、概念理解
### （一）、从 全连接层 到 卷积层
1. 卷积层是一个特殊的全连接层

#### 1、张量形状
1. ***输入、输出*** 好理解
   1. **普通全连接层**是把图片拉成一维向量
      1. 所以**单个数据**的输入、输出都只有一维
   2. 卷积层要保持局部不变
      1. 所以不能把图片拉成一维向量，必须保持高度和宽度
      2. 所以**单个数据**的输入、输出必须是二维 `(宽度, 高度)`
2. 至于 ***权重张量*** 的维度，刚开始，我并不是很理解，后面再想想，好像也能理解？
   1. **普通全连接层**中的权重张量
      1. 首先明确：普通全连接层实际就是 **MLP 中的隐藏层**
      2. ⇒ 那么隐藏层的权重张量就非常好确定了 `(num_hiddens1, num_hiddens2)`
      3. 所以，普通全连接层的权重张量是二维
   2. 卷积层中权重张量
      1. 从全连接层到卷积层的过渡 $$h_{i, j} = \sum_{k, l} ~ w_{i, j, k, l} ~ ~ x_{k, l} = \sum_{a, b} ~ v_{i, j, a, b} ~ ~ x_{i+a, j+b}$$
         1. $h_{i, j}$ 就是卷积层的输出
         2. $x_{k, l}$ 就是卷积层的输入
         3. $w_{i, j}$ 对应**普通全连接层**中的权重，同时 $k, l$ 又对应卷积层中输入的位置关系
         4. ⇒ 这样**一过渡**， $\sum_{k, l} ~ w_{i, j, k, l} ~ ~ x_{k, l}$ 作为卷积层的输出**就很合理**了！
         5. 但是还有一个问题，那就是：为什么输出的下标是 $h_{i, j}$ 而不是 $h_{k, l}$ ？
            1. **数学**上，我认为可以从**全连接层**的角度来理解，下面详细展开
               1. 卷积层本质上还是全连接层，也就是隐藏层
               2. 卷积层的整体框架（**输入、输出**）依然要符合隐藏层
               3. 由于 $k, l$ 是卷积层新加入的东西；$i, j$ 是全连接层本身就有的东西
               4. 因此，卷积层的**输出**张量下标应该是 $i, j$ 而非 $k, l$
               5. **这种解释有点牵强，但不是完全没道理，可以作为一种理解**
            2. 另外就是可以从**卷积层的设计理念**上来理解
               1. 全连接的核心计算逻辑：输出特征图的每一个位置 $h_{i,j}$ ​，都需要和输入特征图的**所有位置** $x_{k,l}$ ​做**加权求和**（ $k,l$ 遍历输入的所有坐标），
               2. 因此 $i,j$ 是输出的**固定坐标**，$k,l$ 是**求和的遍历变量**。
   3. [豆包解答 - 卷积神经网络中，权重张量是四维的原因](https://www.doubao.com/chat/36950674274211074)
3. 理解 ***重新索引***
   1. **数学上**，
      1. 对权重下标的后面 2 位进行一个变换 $$v_{i,j,a,b} = w_{i,j,i+a,j+b}$$
      2. 那么对于原本的卷积公式 $$h_{i, j} = \sum_{k, l} ~ w_{i, j, k, l} ~ ~ x_{k, l}$$ 也对权重下标的后面 2 位进行一个变换，不妨令 $v_{i,j,a,b} = w_{i,j,k,l}$ ， 那么，原式转换成 $$h_{i, j} = \sum_{k, l} ~ w_{i, j, k, l} ~ ~ x_{k, l} = \sum_{a, b} ~ v_{i, j, a, b} ~ ~ x_{c, d}$$ 现在就是要求出 $c, d$ 。结合最开始的坐标变换公式有 $c = i + a, ~ ~ d = j + b$
      3. **数学理解上的关键点**：可以把 $v_{i,j,a,b} = w_{i,j,i+a,j+b}$ 这里的 $a, b \rightarrow m, n$ 。
         1. 即：坐标变换公式为 $v_{i,j,m,n} = w_{i,j,i+m,j+n}$ （用于防止误解！）
         2. 也就是说，坐标变换公式中的 $a, b$ 和 卷积公式中的 $a, b$ 是没关系的。
   2. **卷积神经网络上**，……
<br>

#### 2、参数
1. **平移不变性** （ $x$ 平移不会导致 $h$ 变化 ）
   1. 上面 **经过坐标变换** 后的卷积公式 $$h_{i, j}  = \sum_{a, b} ~ v_{i, j, a, b} ~ ~ x_{i+a, i+b}$$
      1. 由于坐标之间是独立的，我们将 横纵坐标 分开来看，现在只关注 横坐标（因为 纵坐标 和 横坐标 同理）
      2. 平移不变中的 **平移** 意思就是说 $i+a$ 变，但是 $x_{i+a}$ 这个值不变
      3. 平移不变中的 **不变** 意思就是说 输出 $h_{i,j}$ 这个结果不变 （**只关注横坐标**的话，那就是 $i$ 不变！）
      4. **平移不变性** 这个条件转化为数学表述就是： $i+a$ 变化的过程中，$i$ 不变。 ⇒ 那么就 **只能是 $a$ 变， $i$ 不变** ！
      5. ⇒ 平移不变的最终结论是：对于 $h_{i, j}  = \sum_{a, b} ~ v_{i, j, a, b} ~ ~ x_{i+a, i+b}$ 卷积公式，$i, j$ 是常数，并且 $v_{i,j,a,b} = v_{a,b}$
   2. **从未经过坐标变换的卷积公式** 入手，反而更容易推导 $$h_{i, j} = \sum_{k, l} ~ w_{i, j, k, l} ~ ~ x_{k, l}$$
      1. **平移不变性** 这个条件的数学表述就是（仅从横坐标的角度出发）：$k$ 变化的过程中，$i$ 不变
      2. ⇒ 平移不变的最终结论是：对于 $h_{i, j}  = \sum_{k, l} ~ v_{i, j, k, l} ~ ~ x_{k, l}$ 卷积公式，$i, j$ 是常数，并且 $v_{i,j,k,l} = v_{k,l}$
2. **局部性** （这个比较好理解）
   1. 还是先把 卷积公式 摆出来 $$h_{i, j}  = \sum_{a, b} ~ v_{i, j, a, b} ~ ~ x_{i+a, i+b}$$ 
   2. **局部性** 这个条件的要求：评估 $h_{i,j}$ 的时候，不应该选择远离 $x_{i,j}$ 的点
   3. 解决方案：当 $|a|,|b|\gt \Delta$ 时，使得 $v_{a,b}=0$ ，也就是说权重只集中于一个小范围而不是全局。 $$h_{i,j}=\sum_{a=-\Delta}^\Delta\sum_{b=-\Delta}^\Delta v_{a,b} ~ ~ x_{i+a,j+b}$$
<br>

#### 3、总结
1. 平移不变性和局部性都对参数做了限制
   1. ⇒ 因此，卷积神经网络是**经过限制的 MLP**
   2. ⇒ 同时，对参数做限制，也使得模型复杂度降低！
2. 这里实现的操作
   1. 在 **数学** 上其实叫 **二维交叉相关**
   2. 只不过深度学习中，将其称为 **卷积操作**
<br><br>

### （二）、卷积层
#### 1、二维卷积层（二维交叉相关）

![correlation](https://zh.d2l.ai/_images/correlation.svg)

- 输入 $\bf X$ ： $n_h\times n_w$
- 核（kernel）$\bf W$ ： $k_n\times k_w$
- 偏差 $b\in \mathbb{R}$
- 输出 $\bf Y$：$[n_h-(k_h-1)]\times[(n_w-(k_w-1)]$
- $${\bf Y}={\bf X* \bf W}+b$$
  - $\bf *$ 其实就是我们之前定义的二维卷积操作

#### 2、一维 / 二维 / 三维 卷积操作
1. 二维
   1. $$y_{i,j}=\sum_{a=1}^h\sum_{b=1}^w w_{a,b}x_{i+a,j+b}$$
   2. $(i, j)$ 是 **输出特征图** 里面 点的坐标
2. 一维
   1. $$y_i=\sum_{a=1}^hw_ax_{i+a}$$
   2. 多用于文本、语言、时序序列
3. 三维（与 RGB 通道卷积表达式类似）
   1. $$y_{i,j,k}=\sum_{a=1}^h\sum_{b=1}^w \sum_{c=1}^dw_{a,b,c}x_{i+a,j+b,k+c}$$
   2. 多用于含时间维度的视频、气象地图、空间维度医学图像（CT）
<br><br>

### （三）、有意思的问题
- **Q：卷积层的感受野（kernel size）不应该越大越好吗？为什么实际中常见的都是3x3、5x5等小尺寸？**

- **🙋‍♂️**：
   1. 与 MLP 类似，很宽的隐藏层（非常多神经元）不如多层窄一些的隐藏层好训练，
   2. 与之类似，在 CNN 中，尺寸很大的卷积层不如一组尺寸更小但层数更多的卷积层好训练。
   3. 研究表明，由简单的神经元构成的多层结构，有助于将复杂特征的抽取任务“**分而治之，层层击破**”，
   4. 先有底层卷积抽取低级特征（纹理、线条等），再通过层叠结构，越高层的卷积的间接作用范围会随着层数增长而逐渐扩大，最终获得大尺寸下的特征，
   5. 这比一开始就学习一个复杂的特征或模式要更容易。


<br><br><br><br>

## 二、代码实现
### （一）、二维互相关运算的手动实现
```python
'''计算二维互相关运算 correlation'''    
def corr2d(X, K):  #@save
    h, w = K.shape
    Y = torch.zeros((X.shape[0] - h + 1, X.shape[1] - w + 1))
    for i in range(Y.shape[0]):
        for j in range(Y.shape[1]):
            Y[i, j] = (X[i:i + h, j:j + w] * K).sum() 
            # 注意切片的语法；
            # 另外，这里的 * 是点积（真是没想到矩阵还有点积，不过就是模仿向量点积）
    return Y
    # shape 都是先行后列（ 0 轴是从外面开始计算的 ）
```
1. 分析上面这段代码，使用的卷积公式是： $$y_{i,j}=\sum_{a=1}^h\sum_{b=1}^w w_{a,b}x_{i+a,j+b}$$
   1. 看来这个公式比较有实用价值！


