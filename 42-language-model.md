# Language Model | 语言模型
## 一、概念讲解
### （一）、语言模型
- 目前的语言模型，主要思路是给定文本序列 $x_1,...,x_T$，目标为估计其出现的**联合概率** $p(x_1,...,x_T)$
- 联合概率可用于：
  - 做**预训练模型**去 Fine-tune (e.g. BERT, GPT-3)
  - 生成本文，给定前面几个词，不断地使用 $x_t \sim p(x_t|x_1,...,x_{t-1})$ ，生成后续文本
  - 判断多个序列中哪个更常见
    - 在语音识别领域，e.g. "to recognize a speech" vs "to wreck a nice beach"，对于两个发音相近的句子，语言模型可以预测其可能性（合理性）。（先 **语音模型** 再 **语言模型**）
    - 输入法领域也是类似，给定一个键盘输入序列，判断哪种输出更常见

这个领域的重要发展，最早是由 MIT 科学家，后领导了著名的 IBM Watson 实验室的弗里德里克·贾里尼克（Frederick Jelinek，[wiki](https://en.wiki.hancel.org/wiki/Frederick_Jelinek)）提出将**信息论**与**统计方法**成功应用于解决语音识别问题后，带来的整个 NLP 领域方法论的革新。李开复也是贾里尼克的学生。

> 本节课沐神讲的连贯性不是特别好，如果还想了解语言模型相关案例，强烈推荐童鞋阅读吴军老师的科普书《数学之美》第三章，对语言模型的前世今生、工业界（Google）的发展应用都有十分生动有趣的介绍

### （二）、使用计数来建模（与过去 `所有` 点都有关！）
- 这里将计数频率等价于概率有前提，那就是 “**大数定律**”，要求语料要足够大
  - 数理统计中**严格的大数定律**：试验结果的算术平均值具有稳定性
- 假设序列长度为 2，我们预测联合概率 $$p(x,x')=p(x)p(x'|x)={n(x)\over n}{n(x,x')\over n(x)}$$
  - $n$ 是总词数， $n(x),n(x,x')$ 是单个单词和连续单词对（一前一后）的出现次数
- 很容易拓展到长为 3 的情况 $$p(x,x',x'')=p(x)p(x'|x)p(x''|x,x')={n(x)\over n}{n(x,x')\over n(x)}{n(x,x',x'')\over n(x,x')}$$
- 通过扫一遍整个语料库，统计该序列中每个词和其与几个词一起出现的次数，根据大数定律，就可以计算出上述联合概率。
  - 不过理论上虽然可行，但当要求的序列很长时（比如一个长句），
  - 因为搜集语料库成本所限，文本量不够大，很可能造成 $n(x_1,...,x_T)$ 接近于零而无法计算得到该序列的联合概率。
1. 有个 **【问题】** ：为什么不**把中间值约掉**，**直接** 使用 $\frac{n(x, x')}{n}$ 和 $\frac{n(x, x', x'')}{n}$ ？
   1. 【**答**】：虽然 **这里** 确实能把中间值约掉，但是 **N 元模型** 中，中间值就没有办法完全约掉了！（具体看下面 **N 元模型** 的公式即可！）

### （三）、N 元模型（N-gram Model，只与过去 `N-1` 个点有关！）
1. 使用**马尔可夫假设**可以缓解这个问题，该假设可将问题简化为当前词概率只与前一个词（或几个词）有关，与其他词无关，这就避免了求长难句序列的联合概率。虽然该方法丧失了精确性，但是在数学中也广泛应用，效果也不错，不失一种对复杂问题近似逼近的方法。
2. 特别的，
   1. 当只与前一个词有关时，叫做二元模型（Bigram Model），
   2. 与前两个词有关，叫三元模型，
   3. 与前 N-1 个词有关，则称为 N 元模型（N-gram Model），
   4. 马尔可夫假设与真实情况的精度差可以随着 N 的增大而减小。
3. 以计算序列长为 4 的联合概率为例，使用条件概率展开：
   1. 一元模型，即上下文无关：$\tau=0$
      1. $$p(x_1,x_2,x_3,x_4) = p(x_1)p(x_2)p(x_3)p(x_4)$$ 
      2. $$= {n(x_1)\over n}{n(x_2)\over n}{n(x_3)\over n}{n(x_4)\over n}$$
   2. 二元模型，只与前一个词相关：$\tau=1$
      1. $$p(x_1,x_2,x_3,x_4)=p(x_1)p(x_2|x_1)p(x_3|x_2)p(x_4|x_3)$$ 
      2. $$={n(x_1)\over n}{n(x_1,x_2)\over n(x_1)}{n(x_2,x_3)\over n(x_2)}{n(x_3,x_4)\over n(x_3)}$$
      3. 就以二元模型为例，分析一下 **该公式的含义**
      4. 若要求 $p(x_4) = p(x_3, x_2)$，由于是二元模型，所以 $x_3$ 发生的概率只与 $x_2$ 有关，于是 $p(x_3) = p(x_2) p(x_3|x_2)$
         1. 明确：正是因为 $x_3$ 与 $x_2$ 有关 <br>⇒ 导致 **先有的 $p(x_2)$ 概率本身** <br>⇒ **然后才有的条件概率 $p(x_3|x_2)$**。<br>即 $p(x_3) = p(x_2) p(x_3|x_2) = p(x_1) p(x_2|x_1) p(x_3|x_2)$ ！
         2. 推而广之，如果是 **三元模型**，$x_3$ 发生的概率只与 $x_2$ , $x_1$ 相关！<br>⇒ 那么，**先有的 $p(x_1 x_2)$ 概率本身** <br>⇒ **然后才有的 $p(x_3|x_1 x_2)$ 条件概率**。<br>即 $p(x_3) = p(x_1 x_2) p(x_3|x_1 x_2) = p(x_1) p(x_2|x_1) p(x_3|x_1 x_2)$ ！
         3. 注意到 $p(x_3) = p(x_1 x_2) p(x_3|x_1 x_2)$ 不符合数学形式，但是也没问题！因为这里 **是用的马尔科夫假设**，也就是 $x_T$ 只与前面 2 个事件有关，你必须把这两个事件表示出来，那么也就是 $p(x_1 x_2)$ ！<br><br>⇒ 更进一步地思考，如果采用**严格的数学表示**，$x_3$ 应该写成 $x_1 x_2 x_3$，因为 $x_3$ **与前面 2 个事件有关且仅与前面 2 个事件有关** ⇒ 这样一来，严格的数学公式就是 $p(x_1 x_2 x_3) = p(x_1) p(x_2|x_1) p(x_3|x_2 x_1)$<br><br>⇒ 其实 $x_3$ 的例子不够好，因为 $x_2$ 前面的事件数量不足 2 个，没法**继续用马尔科夫假设**。 $x_4$ 的例子更加合适<br>① 先**对 $x_4$ 用马尔科夫假设** $p(x_4) = p(x_4 x_3 x_2) = p(x_2 x_3) p(x_4|x_2 x_3)$<br>② 再**对 $x_3$ 用马尔科夫假设** $p(x_2 x_3) p(x_4|x_2 x_3) = p(x_1 x_2) p(x_3|x_1 x_2) p(x_4|x_2 x_3)$<br>③ 再**对 $x_2$ 用马尔科夫假设**（虽然事件数量已经不满足马尔科夫假设的条件了） $p(x_1 x_2) p(x_3|x_1 x_2) p(x_4|x_2 x_3) = p(x_1) p(x_2|x_2) p(x_3|x_2 x_1) p(x_4|x_3 x_2)$<br>④ 最终得到**三元马尔科夫模型**的前向计算公式 $p(x_4) = p(x_1)p(x_2|x_1)p(x_3|x_1,x_2)p(x_4|x_2,x_3)$
      5. 又由于 $p(x_2) = p(x_1) p(x_2|x_1)$
      6. 所以 $p(x_3) = p(x_1) p(x_2|x_1) p(x_3|x_2)$，在**形式上**看着好像 $x_3$ 发生的概率怎么还与 $x_1$ 有关呢？**实际上**是 $x_3$ 与 $x_2$ 有关，而 $x_2$ 又与 $x_1$ 有关！
   3. 三元模型，只与前两个词相关： $\tau=2$
      1. $$p(x_1,x_2,x_3,x_4) = p(x_1)p(x_2|x_1)p(x_3|x_1,x_2)p(x_4|x_2,x_3)$$ 
      2. $$= {n(x_1)\over n}{n(x_1,x_2)\over n(x_1)}{n(x_1,x_2,x_3)\over n(x_1,x_2)}{n(x_2,x_3,x_4)\over n(x_2,x_3)}$$
4. **N 元模型**相比直接计数来算联合概率的方法，优势在于可以简便计算来近似得到长序列的概率。
   1. 以二元模型为例，近似计算联合概率时，扫一遍语料库，只需要存储一个二元词组 $n(x_i,x_{i-1})$ 和单词 $n(x_i)$ 出现的频数即可近似计算其联合概率。
   2. N 元语法的空间复杂度与 N 的取值是指数关系，即 $O(|V|^N)$ ，这里 $|V|$ 表示一个语言的词汇量。在计算机性能不高的年代，N 取值都不会很大，大家曾经如果用过老式输入法如**智能 ABC**（暴露年龄了…👴）就会秒懂，在输入句子时，大多数情况也只能两个字、两个字地往外蹦，推测用的就是二元模型。
   3. 后期随着计算机性能的提高，输入法使用的 N 才稍稍增大（但也不会很大，不然下载个输入法媲美 3A 大作游戏的体积你是不会答应的），现代输入法也新增 “**云输入**” 的功能，通过在本地输入法运行一个小 N 模型，在云端运行一个大 N 模型，来解决输入长难句的需求。


### （四）、总结
- 语言模型估计文本序列的联合概率
- 使用统计方法时常用 N 元模型（N-gram Model）来近似得到联合概率

<br><br><br><br>

## 二、代码讲解
